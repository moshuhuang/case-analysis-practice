# Capital One Case Interview 模拟面试工具 — 项目计划

> 面向对象：本文档假设你没有编程背景（数据分析/Excel/SQL背景），所有技术名词首次出现时会用中文解释。

## 0. 项目目标

做一个AI模拟面试工具，练习Capital One的case interview（商业分析/数据分析类案例）。核心是两个AI角色：

- **面试官agent**：根据案例材料 + 对话历史 + 你的回答，自主判断"追问"还是"换下一题"，而不是照着固定问题清单念。
- **评估者agent**：对照评分标准（结构化思维、数字/量化敏感度、沟通表达清晰度），指出具体漏洞，生成复盘反馈。

分四个阶段开发，每个阶段做完先自查、再跟你确认，才进入下一阶段。**不会一次性把所有功能做完。**

---

## 1. 技术方案（我的推荐，非你需要做的决定）

| 部分 | 选择 | 为什么 |
|---|---|---|
| 编程语言 | Python | 数据分析背景最容易上手，生态最成熟 |
| 网页界面框架 | **Streamlit** | 纯Python就能做出网页界面，不需要你写HTML/CSS/JS（这三个是网页前端的技术，Streamlit帮你屏蔽掉了） |
| AI大脑 | Anthropic API（Claude模型） | 见下方"费用说明" |
| 默认模型 | Claude Sonnet 4.6 | 推理能力最适合"自主判断追问"这个核心功能；如果后续账单偏高，可切换为Haiku 4.5（更便宜但能力弱一些），这只是一行配置的改动 |
| 阶段三语音 | 浏览器原生 Web Speech API（免费，无需注册） | 语音识别（Speech Recognition，把你说的话转成文字）和语音合成（Text-to-Speech，把AI的回复念出来）都是Chrome/Edge浏览器自带的功能 |
| 阶段三b语音 | Deepgram（语音转文字） + ElevenLabs（文字转语音） | 两者都有免费额度，且不需要绑定月费订阅 |

## 2. 费用说明（请仔细阅读，这是你已确认过的关键前提）

**重要区分**：你已付费的 $20/月 Claude Pro 订阅（网页版/App用）跟这个项目要用的 **Anthropic API**（写程序调用模型用）是两套完全独立的计费系统，互不通用。API是"用多少付多少"。

**费用估算**（基于当前官网定价，2026年7月）：

- 按你"一天1~2次模拟面试"的使用量（约月30~60次），全程使用 Sonnet 4.6，预计每月 **约$4~8美元**。
- 如果改用更便宜的 Haiku 4.5，预计每月 **约$1.3~2.5美元**，但AI的判断力会弱一些。
- 默认方案：**面试官用Sonnet 4.6（保证追问判断质量），评估者视情况可切换Haiku**——如果阶段二做完发现评估质量在Haiku上也够用，可以切换省钱。

**风险控制（阶段一开始前会请你操作）**：
1. 去 Anthropic Console 后台，设置一个"月度花费上限"（spend limit），建议设为$10，超过自动停止调用，避免意外超支。
2. API Key（相当于你的账户密码）绝不会被写入代码或上传到GitHub，会用`.env`文件保存并加入`.gitignore`（一份"不上传清单"）。

## 3. LinkedIn 展示策略

- 代码本身可以**公开发布到GitHub**（作为作品集，别人看代码不花你的钱）。
- **不会**把这个工具部署成一个任何人都能打开链接使用的在线服务——这样别人一用就消耗你的API额度。
- 展示形式是：**本地运行 + 录屏做演示视频**，发布到LinkedIn时附上视频和GitHub链接。阶段三b（Deepgram/ElevenLabs的专业语音）就是为了让这段演示视频质量更好，不涉及公开部署。

---

## 4. 分阶段计划

### 阶段一：纯文字版面试官agent

**做什么**：
- 用Streamlit搭一个简单网页，左侧/上方放案例材料，中间是聊天框
- 你打字回答，面试官agent结合"案例材料 + 完整对话历史 + 你刚才的回答"，用AI自主判断：
  - 追问（针对你回答里的漏洞/模糊点深挖）
  - 或换到案例材料里的下一个子问题
- 需要你提供1~2份公开的Capital One case interview材料（文字形式即可，我来帮你整理成程序能读取的格式）

**不做的事**：评估反馈、语音、demo模式——这些是后面阶段的事

**验收标准**：
- 连续测试至少5轮对话
- 其中面试官至少4次的追问，是针对你回答里的**具体**漏洞或模糊点（比如你说了个数字但没解释假设），而不是"能再详细说说吗"这种通用套话

---

### 阶段二：加入评估者agent

**做什么**：
- 面试整场结束后（不是每问一句都评，省成本也更符合真实体验），评估者agent读取完整对话记录，对照一份评分标准（结构化思维 / 数字敏感度 / 表达清晰度），生成复盘报告
- 评分标准会先写成一份文档给你确认，不是我自己拍脑袋定

**验收标准**：
- 复盘反馈里至少能指出1~2个具体、有理有据的改进点
- 每个改进点都要说明"依据哪个评分维度"（比如："结构化思维方面：你在拆解市场规模时，没有先划分细分客群，直接套用了整体渗透率"）
- 不能出现"回答得不错""继续加油"这种空话

---

### 阶段三：浏览器免费语音交互（日常练习用）

**做什么**：
- 接入浏览器自带的语音识别（听你说话转文字）和语音合成（把AI的话念出来）
- 全程不用任何付费/需要注册的语音服务
- 需要用Chrome或Edge浏览器（Web Speech API在这两个浏览器支持最好）

**验收标准**：
- 能顺畅跑完一整个"你说话 → 转文字 → AI生成回应 → 念出来"的流程，中途不报错
- 至少在Chrome浏览器上验证通过

---

### 阶段三b（可选，仅用于制作LinkedIn演示视频，不影响日常使用）

**做什么**：
- 做成一个**独立的"演示模式"开关**，切换后：
  - 语音识别改用 Deepgram（免费额度，注册送约$200，无需绑定月费）
  - 语音合成改用 ElevenLabs（免费额度：每月10000字符，约10分钟音频）
- 日常练习继续用阶段三的浏览器免费方案，两套方案不互相替换，可以随时切换

**你需要做的事（我会在这一步提醒你）**：
- 分别去Deepgram和ElevenLabs官网注册账号，拿到各自的API Key
- 注意查看两边后台的"免费额度使用情况"，避免用超（尤其如果绑定了信用卡，用超可能产生意外扣费——我会提醒你在两边后台确认是否需要绑卡、以及是否可以设置用量提醒）

**验收标准**：
- 能用这个模式录制一段2~3分钟的完整模拟面试对话演示
- 语音识别准确率、语音自然度相比阶段三有明显、可感知的提升

---

### 阶段四（可选，前面都稳定后再做）

**做什么**：
- 优化语音的自然度
- 支持"打断"（你说话时AI能停下来听你说，而不是必须等AI说完）

**验收标准**：待阶段三b完成后，根据实际体验再具体定义（打断功能涉及的技术复杂度较高，届时会先评估可行性再定验收标准）

---

## 5. 工作方式提醒

- 每完成一个阶段，我会先对照本计划自查一遍有没有遗漏/偏离，再找你确认是否进入下一阶段
- 全程用中文解释，英文技术名词第一次出现时会附中文解释
- 涉及花钱/注册账号/公开部署的操作，会提前跟你说清楚，等你确认才动手

---

**请你审阅以上内容，确认没问题（或提出修改意见）后，我再开始阶段一的开发。**
